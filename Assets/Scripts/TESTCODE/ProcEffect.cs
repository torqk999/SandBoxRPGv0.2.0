using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//[CreateAssetMenu(fileName = "ProcEffect", menuName = "ScriptableObjects/Effect/Proc")]
/*public class ProcEffect : BaseEffect
{
    [Header("Proc Properties")]
    public ParticleSystem Impact;

    public float DurationLength;
    public float Timer;

    public override void CloneEffect(BaseEffect source, int equipId = -1, float amp = 1, bool inject = true)
    {
        base.CloneEffect(source, equipId = -1, amp = 1, inject = true);

        if (!(source is ProcEffect))
            return;

        ProcEffect procSource = (ProcEffect)source;

        Impact = procSource.Impact;
        DurationLength = procSource.DurationLength;
        Timer = DurationLength;
    }
}*/

using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/*[System.Serializable]
public class OccupantPanel : Panel
{
    public List<DraggableButton> List;

    public override void Resize(int size)
    {
        List<DraggableButton> newList = new List<DraggableButton>(size);

        for (int i = 0; i < newList.Capacity || i < List.Count; i++)
        {
            if (i < List.Count)
                newList.Add(List[i]);
            else
                newList.Add(null);
        }
            
        List = newList;
    }
}*/

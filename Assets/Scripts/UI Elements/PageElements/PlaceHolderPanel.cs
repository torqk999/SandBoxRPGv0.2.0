using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/*public class PlaceHolderPanel : Panel
{
    public List<PlaceHolderButton> List;

    public override void Resize(int size)
    {
        List<PlaceHolderButton> newList = new List<PlaceHolderButton>(size);

        for (int i = 0; i < newList.Capacity || i < List.Count; i++)
        {
            if (i < List.Count)
                newList.Add(List[i]);
            else
                newList.Add(null);
        }

        List = newList;
    }
}*/
